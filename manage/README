remote console management

- run `install`
- copy `template` into a new script called <server_name>
- edit server IP inside the script
- run `run <server_name>`, where server_name is the name of new script
